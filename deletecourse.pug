html
    body
        h1 #{appName}
        h2 Delete Courses
        form(action="/deletecourse" method="post")
            label Course Name
            input(type="text" name="course")
            input(type="submit" value="Delete")